- content_for :title, '25c - ' + t('.title')

%h2.page-header= t('.title')
%p= t('.message_p1', :amount => number_to_currency(params['mc_gross'].to_f))
%p= t('.message_p2', :amount => number_to_currency(params['mc_gross'].to_f))
%p= t('.message_p3')
