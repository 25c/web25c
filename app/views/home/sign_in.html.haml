- content_for :title, t('.title')
.page-header
  %h1= yield :title
= simple_form_for @user, :url => sign_in_path, :html => { :class => 'form-horizontal' } do |f|
  = f.input :email
  = f.input :password
  .form-actions
    = f.button :submit, t('form.submit'), :class => 'btn-primary'
  