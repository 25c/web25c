=content_for(:js) do
  :javascript
    $(document).ready(function() {
      var linkText = {
        'about_more': '#{ t('.about_more')}',
        'about_less': '#{ t('.about_less')}',
        'button_more': '#{ t('.button_more')}',
        'button_less': '#{ t('.button_less')}',
        'url_more': '#{ t('.url_more')}',
        'url_less': '#{ t('.url_less')}'
      };
      
      $('.more-link').click(function(e) {
        e.preventDefault();
        var $link = $(this);
        var target = $link.attr("href").substring(1);
        $('.more-content[name="' + target + '"]').slideToggle('slow', function() {
          if ($(this).is(':visible')) $link.text(linkText[target + '_less']);
          else $link.text(linkText[target + '_more']);
        });
        window.location.hash = target;
      });
      
      // var hash = window.location.hash.substring(1);
      // if ($.inArray(hash, ['button', 'about']) != -1) {
      //   var $link = $('a.more-link[href$="' + hash + '"]');
      //   $link.click();
      //   $(window).scrollTop($link.parent().offset().top - 40) 
      // }
    });

.splash
  .row.buffer
    .span11
      %h1#splash-title= t('.value_proposition')
    .span4#splash-left
      #text1.splash-text
        %p.lead= raw t('.value_proposition_sub1q')
        %p.lead= raw t('.value_proposition_sub1a')
      %div#splash-buttons
        =link_to t('.call_to_action'), sign_in_path, :class => 'lead btn btn-large btn-warning', 
          :id => 'action-button'
    .span7
      %div{:style => 'height: 250px; width: 500px; overflow: hidden'}
        #value-div{:style => 'position: relative'}
          %img#flow1{:src => image_path('flow1.png')}
  .row
    .span11
      %p.lead= raw t('.about_25c_p1')
      %p.lead= raw t('.about_25c_p3')

.row.main-content
  .span11
    %h2= raw t('.button_25c_title')
    %p
    .more-content{:name => 'button'}
  =render 'home/buttons/form'
  =render 'home/buttons/platforms'
  .clear
  / %p.lead= link_to t('.button_more'), '#button', :class => 'more-link'
